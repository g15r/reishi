---
description: Conventions for HTTP API design and naming.
priority: 10
---

# API Conventions

Use kebab-case for URL paths and snake_case for JSON fields. Every endpoint
must return a JSON envelope with `data` and `meta` keys.
