# none-detected fixture

Plain project root with no agent footprint and no recognised index filename —
asserts the discovery scanner reports an empty result and the link flow takes
the no-prompt path.
