# deeply-nested fixture

Top-level looks like an ordinary project. The agent footprint is buried under
`some/deep/nest/.claude/`. Used to assert the scoop scanner does not recurse
past the supported depth.
