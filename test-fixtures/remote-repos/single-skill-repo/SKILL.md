---
name: single-skill-repo
description: A test fixture skill that lives at the root of its repository, used for exercising single-skill add flows offline.
---

# Single Skill Repo

Fixture skill at the root of the repo. Exists only for reishi integration tests.

## Overview

A minimal but valid skill layout. Enough to pass `validate` so add tests can round-trip
through the normal install path without hitting the real GitHub API.
